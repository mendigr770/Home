import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#216869',
    },
    secondary: {
      main: '#9cc5a1',
    },
    error: {
      main: '#ff6b6b',
    },
    text: {
      primary: '#1f2421',
    },
  },
});

export default theme;